---
title: 一、面向对象
published: 2023-05-30
pinned: false
description: "Java基础"
image: "./cover.avif"
tags: ["Java", "博客", "技术总结"]
category: Java SE
draft: false
---

# 1.发生重载的条件（2023.5.30）

条件1：在同一个类中

条件2：方法名相同

条件3：参数列表不同

​				1.参数个数不同；

​				2.参数的类型不同；

​				3.参数的顺序不同。

要同时满足上述三个条件，才能算作发生方法重载

（注：方法重载与返回值类型无关，与修饰符列表也无关（public static）如，同一个类中出现了

``` java
public static int m5(){
    return 1;
}
public static double m5(){
    return 1.0;
}
```

该情况属于方法重复，而不是重载

）

jvm只能理解.class文件，所以需要先将.java文件编译成.class文件，jvm才能识别并运行

# 2.对象和引用(2023.05.31)

## 方法区

方法区内，存代码片段。

方法区是最先有数据的，因为类最先被加载；

## 栈区

存放的内容：方法调用（压栈），该方法需要的内存在栈中分配，栈中最主要存储局部变量

局部变量是在方法中的

## 堆内存

堆内存中存放的是new的对象，凡是通过new运算符创建的对象，都存储在堆内存当中。

new运算符的作用就是在堆内存当中开辟一段空间，用来存放new的对象.

堆内存中还存放对象的实例变量



``` java
public class StudentTest {
    public static void main(String[] args){
        Student s1 = new Student();   //和int i = 10;是一个道理
        
    }
}
```

该程序中，先将StudentTes.classt类存放在方法区中装载，然后将String.class方法装载在方法区中

还有Student.class，他们的代码字节码

# 3.面向过程的开发方式(2023.06.02)

注重步骤，注重的是实现这个功能的步骤，

第一步干什么，第二步干什么，面向过程也注重实现功能的因果关系

因为a所以有b，因为b所以有c，因为c所以有d

## 缺点

面向过程穿插了大量的因果关系，若其中的一项因果关系出现问题，那么整个程序都将出现问题

代码和代码之间的耦合度太高，扩展力太差

## 优点

对于小型项目，效率较高，不需要前期进行对对象的提取以及模型的建立，采用面向过程的方式直接开始干活，一上来就直接写代码，编写因果关系，从而实现功能

成本低，效率高

# 面向对象的开发方式

面向对象的开发方式更符合人类的思维方式，人类就是以“对象”的方式认识世界的，

面向对象就是将现实世界分割成不同的单元，然后每一个单元都实现成对象，进行驱动，最后将每个对象结合在一起，协同起来一起工作，并且每个对象可以更换，

优点：耦合度底，扩展力强。





OOA：面向对象分析

OOD：面向对象分析

OOP：面向对象编程



## 面向对象三大特征

封装，继承，多态



## 类和对象

类：模板

对象：是通过类创造的个体

引用：Address a，这个“a”就是引用

实例：对象的别称

实例化：通过这个类的模板，创建对象的过程，叫做，实例化。

抽象：多个对象具有的共同特征，进行思考总结抽取具体特征的过程。

类=属性（状态）+方法（动作）

属性：是类的成员变量

# 4.构造方法需要掌握的知识点

1.构造方法有什么作用？

能够创建对象，同时能够给成员变量（实例变量）属性赋值，完成属性的初始化

2.构造方法怎么定义？语法是什么？



3.构造方法怎么调用，使用哪个运算符？

4.什么是缺省构造器？

无参数的构造方法

5.怎么防止缺省构造器丢失？

将无参数的构造方法写出来

6.实例变量在类加载是初始化吗？实例变量在什么时候初始化？

不是，是在构造方法调用的时候

# 5.面向对象的三大特征

## 封装、继承、多态

有了封装才有继承，有了继承才能说多态

## 1.封装

privat封装过于安全，因此需要一个接口来实现读数据和写数据，同时保证数据安全；

因此需要用到get和set函数接口

# 6.静态(2023.06.03)

java中一共有三个变量，

局部变量，在栈区中

实例变量，在堆内存中

静态变量，在方法区当中

## 什么时候变量声明为实例的，什么时候声明为静态的？

​		如果这个类型的所有对象的某个属性值都是一样的，不建议定义为实例变量，浪费内存空间。建议定义为类级别特征，定义为静态变量，在方法区中只保留一份，节省内存开销。

一个对象一份的是实例变量

所有对象一份，是静态变量

## 静态代码块

用static定义

``` java
static {
    java语句
}
```

类加载时执行静态代码块，

且只执行一次，并且在main方法执行之前执行，静态代码块一般按照自上而下的顺序执行

## 实例代码块

代码格式

``` java
{
    java语句
}
```

在每次调用构造方法前都会执行一次实例代码块

## 执行顺序

静态代码块--》实例代码块--》构造函数

# 7.this(2023.06.04)

是关键字，全部小写

静态方法的调用不需要对象，所以不需要this调用

this为什么不能用在静态方法中？

答：因为this是当前对象

## this什么时候不能省略

形式参数（局部变量）和实例变量名称一样时，实例变量前必须加this.

举例：

``` java
public void setName(String name){
    this.name = name;
}
```

this 还可以通过一个构造方法调用本类的另一个构造方法

语法格式 this(实际参数列表)

this()这种语法只能出现在构造方法的第一行，表示当前构造方法调用本类其他的构造方法，目的是代码复用

# 8.继承extends(2023.06.09)

除了父类的构造方法外，可以继承给子类。

私有类可以继承，但是在子类中是无法直接访问的

## 有了继承关系，才有了方法的覆盖和多态

## 没有继承，就没有多态

java中的继承只支持单继承，不支持多继承，c++可以多继承

c++其实更接近现实，因为在现实世界中，儿子同时继承父母两方特征

如：class C extends A,B{ }  这是错误的!

但可以产生间接继承的效果

如：class A{}

class B extends A{}       class C extends B{}

class B == class B extends object;

object类，是所有类的超类，是老祖宗，是根

Class Object is the root of the class hierarchy. Every class has Object as a superclass. All objects, including arrays, implement the methods of this class.

子类继承父类后，可以调用父类的方法吗？

当然可以，因为子类继承父类后，那就是子类的方法了

toString()方法是将对象转换成字符串

# 9.方法覆盖(2023.06.09)

## 什么时候考虑方法覆盖？

当子类继承父类之后，继承过来的方法无法满足当前子类的业务需求时，子类有权利对这个方法进行重新编写，有必要进行“方法的覆盖”。

重要结论：当子类对父类继承过来的方法进行方法覆盖之后，子类对象调用该方法的时候，一定执行子类覆盖之后的方法，之前的方法就不执行了



## 方法覆盖发生的条件

1.两个类必须是继承关系

2.重写之后的方法和之前的方法具有相同的：返回值类型，方法名，形式参数列表

3.访问权限不能更低，可以更高。访问权限由低到高：private -> protected -> public（私有方法无法覆盖）

4.重写之后的方法，不能比之前的方法抛出更多的异常，可以更少



## 注意（以下注意事项，在学习了多态语法之后自然就明白了）

1.方法覆盖只针对于方法，和属性无关

2.私有方法无法覆盖

3.构造方法不能被继承，所以构造方法也不能被覆盖

4.方法覆盖只是针对于实例方法，静态方法覆盖没有意义



object类中有写一个toString（）方法，该方法返回的是字符串，在调用时返回的是对象的内存地址

``` java
public class{
    public static void main(String[] args){
		//创建对象的语句...
        System.out.println(对象.toString());
        System.out.println(对象);   //会默认调用toString()方法
        //返回的值是一样的
    }
}
```

因此，在方法覆盖时，要注意不要写错，（建议复制粘贴），在进行方法覆盖后，

``` java
 System.out.println(对象); 
```

该语句也会默认调用方法覆盖后的toString()方法；

方法覆盖和之后要讲的多态机制有关系，多态的机制，让程序具有很强的扩展性

# 10.多态(2023.06.12)

## 多态的基础语法

1.学习多态基础语法之前，我们需要普及两个概念

​	第一个：向上转型 （子---》父）（自动类型转换）

​	第二个：向下转型（父---》子）（强制类型转换，需要加强制类型转换符（））

注意：Java允许向上和向下转型，

​	*****无论是向上转型还是向下转型，两种类型之间必须有继承关系，没有继承关系编译器将会报错

2.多态指的是：

​	父类型引用指向子类型对象

​	包括编译阶段和运行阶段

​	编译阶段：绑定父类的方法

​	运行阶段：动态绑定子类型对象的方法

3.什么时候必须使用“向下转型”？

​	如果你想访问的方法是子类对象中特有的方法，是父类中没有的方法，此时需要进行向下转型

向下转型就是进行强制类型转换

``` java
class Cat extend Animal{
    public void catchMouse(){
        System.out.println("猫在抓老鼠！！")
    }           //该方法是子类Cat中特有的
    public void move(){
        Sysrem.out.println("猫在移动！！！")
    }
}
class Animal{
    public void move(){
        System.out.println("动物在移动！！")
    }
}
public class abc{
    public static void main(String[] args){
        Animal a1 = new Cat();    //向上转型，编译可以通过
        a1.move();          //该方法move 父类Animal中存在，因此可以编译通过
        a1.catchMouse();      //该方法catchMouse 父类中不存在，是Cat类中特有的，因此如果想调用该方法，应当进行“强制类型转换”，即向下转型。
       ((Cat) a1).catchMouse();   //正确了
    }
}
```





4.向上转型（子---》父）

父类型的引用，允许指向子类型中的对象

Animal a2 = new Cat();
Animal a3 = new Bird();

a2,a3 是父类型中的引用，new cat()是一个子类型中的对象。

***编译阶段不会去new对象





### 新的运算符(instanceof)

第一：instanceof可以在运行阶段**动态判断**引用指向的对象的类型；

第二：instanceof的语法：

​			（引用   instanceof    类型）

第三：instanceof运算符的运算结果只能是：true/false;

第四：假设（c instanceof Cat）为true，表示：

​			c引用指向的堆内存中的java对象是一个Cat

​			假设（c instanceof Cat）为false，表示：

​			c引用指向的堆内存中的java对象不是一个Cat

为什么要使用instanceof运算符？

答：可能一个程序不是同一个人编写的，比如你写了一个有参方法，另一个人调用了你编写的这个方法，而这个参数你不知道是什么类型，这时就需要使用该运算符进行判断操作，从而进行对应类型内的操作。

## 多态在开发中的作用

非常重要***

使程序的耦合度降低，提高程序的扩展力

面向对象三大特征：

封装，继承，多态

有了封装，有了这种整体的概念之后，对象和对象之间才能产生继承关系，有了继承之后，才有了方法的覆盖，从而产生多态。



## 这里提到了软件开发的一条原则

七大原则最基本的原则：ocp原则（即对扩展开放，对修改关闭）

目的是：降低程序耦合，提高程序的可扩展性

要面向抽象编程，最好不要面向具体编程





## 静态方法不存在方法覆盖

1.方法覆盖需要和多态机制联合起来使用才有意义。
Animal a = new Cat();
a.move();
该语句要的是什么效果？
编译的时候move()方法是Animal的，运行时自动调用到子类重写的move()方法上。
没有多态机制，方法覆盖可有可无
如果父类的方法无法满足子类的业务需求的时候，子类完全可以定义一个新的方法。
方法覆盖和多态密不可分
2.静态方法存在覆盖吗？
静态方法使用时是：类名.
而不是：引用.
静态方法的执行不需要对象，所以一般情况下，我们会说静态方法“不存在”方法覆盖

## 私有方法不能覆盖

## 关于方法覆盖的返回值类型

方法覆盖时返回值类型可以修改但之间要有继承关系且只能大变小，不能小转大，返回值类型可以变得更小，但意义不大

# 11.super关键字(2023.07.12)

（super 和 this 对比着学习）

1.this能出现在构造方法和实例方法中。

this的语法：“this.”、“this()”

this不能使用在静态方法中

this.在大部分情况下是可以省略的

在区分局部变量和实例变量时不能省略

``` java
public void setName(String name){
    this.name = name;
}
```

this()只能出现在构造方法第一行，通过当前的构造方法去调用“本类”中其他的构造方法，目的是代码复用。

2.super

super能出现在构造方法和实例方法中。

super的语法：“super.”、“super()”

super不能使用在静态方法中

super.在大部分情况下是可以省略的

## 在什么情况下不能省略？

如果父类和子类中有同名属性，并且你希望在子类中访问到父类的该属性，则需要在该子类方法中属性前添加**super.**，super.不能省略。



2.super()只能出现在构造方法第一行，通过当前的构造方法去调用“**父**类”中的构造方法，

目的是：创建子类对象的时候，先初始化父类型特征。

3.super（）表示通过子类的构造方法调用父类的构造方法，模拟现实中的场景，要想有儿子，得先有父亲。

4.**重要结论：**

当一个构造方法的第一行：

既没有this（）又没有super（）的话，就会默认有一个super（）；

表示通过当前子类的构造方法调用父类的无参构造方法；

所以必须保证父类存在一个无参数构造方法；

5.**注意：**

this（）和super（）不能共存，他们都只能出现在构造方法的第一行。

6.无论怎样折腾，父类的构造方法一定都会执行。

## java是怎么区分子类和父类的同名属性的

是通过this.name

super.name

# 12.final关键字(2023.07.12)

1.final表示最终的，不可变的

2.final可以修饰变量，方法，类

## final修饰的变量？

final修饰的局部变量首次赋值后，不能再次对该变量进行赋值。

## final修饰的方法？

final修饰的方法无法被覆盖。

## final修饰的类？

final修饰的类无法被继承。

## final修饰的引用？

该引用只能指向一个对象，并且它只能永远指向该对象，无法再指向其他对象。并且在该方法执行过程中，该引用指向对象之后，该对象不会被垃圾回收站回收，直到当前方法结束，才会释放空间。

虽然final的引用指向对象A后，不能再重新指向对象B，但是对象A内部的数据是可以被改变的

## final修饰的实例变量？

因为在实例变量中，系统会默认给实例变量赋初值，如果加上final，则程序员必须手动给实例变量赋初值，以防该变量一直是默认值无法修改。

这个手动赋值，在实例变量后面赋值可以，在构造方法中赋值也可以。

**static final 联合修饰的变量称为“常量”，常量名建议全部大写，每个单词之间用下划线衔接**

**常量和静态变量都是存储在方法区，并且都是在类加载时初始化。**

常量和静态变量的区别：

常量的值不能变

# 13.抽象类(2023.07.12)

抽象类中不一定有抽象方法，抽象方法必须出现在抽象类中

**重要结论：一个非抽象类继承抽象类，必须将抽象类中的抽象方法实现了**

（这里的覆盖或者说重写，也可以叫做实现，对抽象的实现）

理解：要变得具体，所以在子类中要将父类的抽象方法实现

抽象类不能创建对象。



## 总结

1.抽象类如何定义？ 在class前加abstract关键字就可以

2.抽象类是无法实例化的，不可以创建对象，所以抽象类是用来被子类继承的

3.final和abstract不能联合使用，因为这两个关键字是对立的

4.抽象类的子类可以是抽象类，也可以是非抽象类

5.抽象类虽然无法实例化，但是抽象类有构造方法，这个构造方法是供子类使用的

6.抽象类中不一定有抽象方法，但抽象方法一定在抽象类中

7.抽象方法如何定义？

public abstract void xxx();

**8.一个非抽象类，继承抽象类，必须将抽象类的抽象方法进行覆盖/重写（实现）**

面试题（判断）：java语言中凡是没有方法体的方法都是抽象方法。

Object类中就有很多方法都没有方法体，都是以；结束的，但他们都不是抽象方法，

例如：public native int hashCode();

该方法底层调用了C++写的动态链接库程序。

前面修饰符列表中没有：abstract，有一个native。表示调用jvm本地程序。

# 14.接口(2023.07.13)

## 接口的基础语法：

1.接口也是一种“引用数据类型”，编译之后也是一个class字节码文件。

2.接口是完全抽象的。（抽象类是半抽象）或者也可以说接口是特殊的抽象类

3.接口怎么定义？语法是什么？

[修饰符列表] interface 接口名（）

4.接口支持多继承，一个接口可以继承多个接口。

5.接口中只包含两部分内容，一部分是常量，一部分是抽象方法

6.接口中所有元素都是public修饰的（都是公开的）

7.接口的抽象方法定义时：public abstract修饰符可以省略

8.接口中的方法都是抽象方法，所以接口中的方法不能有方法体

9.接口中的常量的public static final可以省略。



## 接口的基础语法？

1.类和类之间叫做继承，类和接口之间叫做实现。

别多想：你仍可以将“实现”看作“继承”

继承使用extends关键字完成。

实现使用implements关键字完成。

**2.当一个非抽象的类实现接口的话，必须将接口中所有的抽象方法全部实现（覆盖，重写）**

**3.实现过程中，必须在方法前用public修饰，访问权限不能更低（private<缺省<protected<public）**

4.接口和接口之间支持多继承

``` java
interface C extends A,B{
    
}
```

​     类和接口之间也可以支持多实现

``` java
class C implements A,B{
    `	
}
```



**5.一个类可以同时实现多个接口**

**这种机制弥补了Java中的哪个缺陷？**

Java中类和类之间只支持单继承，实际上单继承是为了简单而出现的，现实世界中存在多继承，Java中的接口弥补了单继承带来的缺陷。实现多个接口，其实就实现了多继承，

6.继承和实现都出现的时候，先继承再实现

``` java
class Cat extends Animal implements Flyable{
    
}
```

7.使用接口时可以多态

## 接口在开发中的作用？

（解耦合）

接口在开发中的作用类似于多态在开发中的作用

多态：面向抽象编程，不要面向具体编程，降低程序的耦合度，提高程序的扩展力

面向接口编程（）

有了接口就有了可插拔，可插拔：表示扩展力强，不是焊接死的。

举例：主板和内存条之间有插槽，这个插槽就是接口，内存条坏了，可以重新买一个换新的，这叫做高扩展性。

**分析：

假如去餐馆吃饭，这个过程的接口是：菜单

A.菜单上有每个菜品的照片（接口）

B.顾客挑选菜品（调用接口的过程）

C.厨师来做这道菜（接口的具体实现）

这个“菜单”接口有什么用呢？

答：  这个菜单，使顾客和厨师之间**解耦合**了。

​         顾客不用找后厨，后厨不用找顾客，他们之间的沟通的桥梁完全依靠菜单这个接口。





## 抽象类和接口的区别

1.抽象类是半抽象的，接口是完全抽象的

2.抽象类有构造方法，接口没有构造方法

3.接口和接口之间支持多继承，类和类之间只能单继承

4.一个类可以同时实现多个接口，一个抽象类只能继承一个类（单继承）

5.接口中只允许出现常量和抽象方法。

*以后接口使用的比抽象多，一般抽象类使用的还是少，接口一般都是对”行为“的抽象。*

# 15.关于Java中package和import机制

## package

1.使用package的目的是为了方便程序的管理，不同功能的类分别存放在不同的包下。

2.package怎么用？

它是关键字，后面加包名

3.命名规范：

一般采用公司域名倒序的方式（公司域名具有全球唯一性）

规范：公司域名倒叙+项目名+模块名+功能名

## import

什么时候使用import？

在不同的包下，访问另外一个包中的类时，需要使用import。



举例：

``` java
public static void main(String[] args){
    java.util.Scanner s = new java.util.Scanner(System.in);  //java.util就是包名
}
```

可以在类的前面进行导包，导入其他包中的类，即Scanner类

``` java
import java.util.Scanner;
    public static void main(String[] args){
    Scanner s = new Scanner(System.in);
}
```

** **lang包下根目录的类不需要手动导入（java.lang.\*）**

# 16.访问控制权限(2023.07.14)

## 访问控制权限有哪些？

共4个：private 私有

​			protected 受保护

​			public  公开

​			     		默认

private：只能在本类中访问

public：在任何位置都能访问。

protected：表示只能在本类，同包，异包子类中访问

默认:  表示只能在本类，同包下访问。

## 访问控制权限修饰符可以修饰什么？

属性，方法，（四个都可以）

类，接口（public和默认能用，其他不行）

# 17.Object类

## object类中会有一些常用方法：





## 什么是API？

应用程序编程接口，整个jdk的类库就是一个Javase的api，每个api都会配置一套api帮助文档，sun公司提前写好的这套类库就是api（一般每一份api都会对应一份api帮助文档）



我们需要掌握的方法：

protected  Object clone()    //负责对象克隆的

int hashCode()   //获取对象哈希值的一个方法

boolean equals (Object obj)    //判断两个对象是否相等

String toString( )     //将对象转化成字符串形式

protected void finalized()      //垃圾回收器负责调用的方法

## Object 类中的toString（）方法

### 1.源代码长什么样？

``` java
 public String toString() {
        return getClass().getName() + "@" + Integer.toHexString(hashCode());
    }
//源代码上toString()方法的默认实现是：类名@对象的内存地址转换为十六进制的形式
```

### 2. sun公司设计toString方法的目的是什么？

通过调用这个方法，可以将一个“Java对象”转化成“字符串表示形式”。

### 3.建议所有的子类都去重写toString方法

注：**输出引用，会默认调用toString（）方法**

``` java
public class Test01 {
    public static void main(String[] args) {
        MyTime m1 = new MyTime(2003,12,25);

        System.out.println(m1);
    }
}
```

## Object类中的equals()方法

### 1.equals源代码

``` java
public boolean equals(Object obj) {
        return (this == obj);
    }
//以上方法是Object类的默认实现
```

### 2. sun公司设计equals方法的目的：

以后编程的过程当中，都要通过equals方法判断两个对象是否相等

### 3.我们需研究Object类给的这个默认的equals方法够不够用！



## Object类中的finalize()方法

### 1.finalize源代码

``` java
protected void finalize() throws Throwable { }
```

### 2.这个方法只有一个方法体，里面没有代码，而且这个方法是protected修饰的。

### 3.这个方法不需要程序员手动调用，jvm的垃圾回收器负责调用这个方法。

### 4.finalize（）方法执行的时机：

当一个Java对象即将被垃圾回收器回收时，垃圾回收器负责调用finalize（）方法。

### 5.finalize（）方法实际上是sun公司为Java程序员准备的一个时机，垃圾销毁时机，如果希望在对象销毁时机执行一段代码的话，这段代码要写到finalize（）方法中。

### 6.静态代码块的作用？

static{

}

静态代码块在类加载时刻执行，并且只执行一次，这是sun准备的类加载时机。

同样，finalize（）方法也是sun公司为程序员准备的一个时机，这个时机是垃圾回收时机。

### 7.Java中的垃圾回收器不是轻易启动的，垃圾太少或时间没到，等种种条件下，有可能启动，也有可能不启动（System.*gc*();    //建议启动垃圾回收器语法，提高启动的概率）

## Object类中的hashCode（）方法

### 1.源代码

``` java 
public native int hashCode();
```

该方法不是抽象方法，带有native关键字，底层调用c++程序

### 2.hashCode()方法返回的是哈希码：

​	实际上就是一个Java对象的内存地址，经过哈希算法，得出的一个值。

​	所以hashCode（）方法的执行结果可以等同的看作一个Java对象的内存地址

# 18.匿名内部类

## 1.什么是内部类?

在类的内部又定义了一个新的类，被称为内部类。

## 2.内部类的分类

静态内部类：类似于静态变量

实例内部类：类似于实例变量

局部内部类：类似于局部变量

## 3.使用内部类编写的代码可读性很差，能不用尽量不用

## 4.匿名内部类：

匿名内部类是局部内部类的一种

因为这个类没有名字而得名，叫做匿名内部类

## 5. 学习匿名内部类是为了在阅读别人写的代码时能够理解

缺点1：太复杂，太乱，可读性差

2：因为一个类没有名字，没办法重复使用












